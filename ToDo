comments styles

refactor meta reducers (merge in a single file, reuse some code?)
refactor reply added reducer to use expanding reducer

clamp aticle description in a list in a better way

pluralize comment(s) in article list

add animation

load nice fonts
